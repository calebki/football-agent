[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "fantasy-agent"
version = "0.0.1"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
namespaces = true
